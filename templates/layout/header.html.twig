<header class="header">
  <nav class="navbar">
    <a href="{{ path('index') }}"><h1>PhotoMap</h1></a>
    <ul class="navbar-nav">
      {% if app.user %}
        <li class="nav-item">
          <a href="{{ path('logout') }}" class="nav-link">Odhlásit</a>
        </li>
        <li class="nav-item">
          <a href="{# TODO: Add link to post creation #}" class="btn btn-sm btn-primary">Přidat místo</a>
        </li>
      {% else %}
        <li class="nav-item">
          <a href="{{ path('login') }}" class="nav-link">Přihlásit</a>
        </li>
        <li class="nav-item">
          <a href="{# TODO: Add link to registration #}" class="nav-link">Registrovat</a>
        </li>
      {% endif %}
    </ul>
  </nav>
</header>