<header class="header">
  <nav class="navbar">
    <a href="{{ path('index') }}" class="logo">PhotoMap</a>
    <ul class="navbar-nav">
      {% if app.user %}
        <li class="nav-item">
          <a href="{{ path('logout') }}" class="nav-link">Odhlásit</a>
        </li>
        <li class="nav-item">
          <a href="{{ path('post_add') }}" class="btn btn-sm btn-primary">Přidat <span>místo</span></a>
        </li>
      {% else %}
        <li class="nav-item">
          <a href="{{ path('login') }}" class="nav-link">Přihlásit</a>
        </li>
      {% endif %}
    </ul>
  </nav>
</header>